apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: dev-payment-manager-frontend
spec:
  template:
    spec:
      containers:
        - image: asia-northeast1-docker.pkg.dev/common-payment-manager/main/payment-manager-frontend:12260829
          ports:
            - containerPort: 3000
